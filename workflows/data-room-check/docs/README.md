# data-room-check (human guide)
Audit a read-only reference folder against a checklist; writes a report into output/.
`python scripts/audit_folder.py "<reference folder (read-only)>" references/checklist.example.md output`
