# GitHub Copilot shim

The instructions for this workspace live in `/AGENTS.md`. Read it first and follow it.

(Only GitHub Copilot auto-loads this file. Every other tool — Codex, Claude, a CLI — should read
`AGENTS.md` directly. This shim deliberately contains no rules of its own, so there is one single
source of truth.)
