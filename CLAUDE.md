# Claude shim

The instructions for this workspace live in `/AGENTS.md`. Read it first and follow it.

(Claude Code auto-loads this file. It deliberately contains no rules of its own — `AGENTS.md` is
the single source of truth. Same pattern as `.github/copilot-instructions.md` for Copilot.)
